'/bootstrap':signature(queryPat(Now,Query)::'/parsing':pattern('/parsing':token),[Now::dayTime,Query::query]).
'/bootstrap':signature(city(Name)::'/parsing':pattern('/parsing':token),[Name::'/bootstrap':string]).
'/parsing':prod(city(Name),'/parsing':token,'/parsing':terminal('/parsing':word(Name))).
'/parsing':prod(city(Name),'/parsing':token,'/parsing':follow('/parsing':terminal('/parsing':word(FirstWord)),'/parsing':follow('/parsing':terminal('/parsing':word(SecondWord)),'/parsing':pred('/Functional':eval('/Functional':applyBinOp('/Functional':const(FirstWord),'/Functional':concat,'/Functional':applyBinOp('/Functional':const(!' '),'/Functional':concat,'/Functional':const(SecondWord))),'/bootstrap':string,Name))))).
annotation:shrink('/parsing':prod(queryPat(Now,forwardQuery(From,DepartureTime,To)),'/parsing':token,'/parsing':follow(dayTimePat(Now,DepartureTime),'/parsing':follow('/parsing':opt('/parsing':terminal('/parsing':word(!from))),'/parsing':follow(city(From),'/parsing':follow('/parsing':terminal('/parsing':word(!to)),city(To))))))).
annotation:shrink('/parsing':prod(queryPat(Now,forwardQuery(From,DepartureTime,To)),'/parsing':token,'/parsing':follow('/parsing':opt('/parsing':terminal('/parsing':word(!from))),'/parsing':follow(city(From),'/parsing':follow('/parsing':terminal('/parsing':word(!to)),'/parsing':follow(city(To),'/parsing':follow(annotation:parentheses('/parsing':alt('/parsing':alt('/parsing':terminal('/parsing':word(!on)),'/parsing':terminal('/parsing':word(!at))),'/parsing':follow('/parsing':terminal('/parsing':word(!departing)),'/parsing':opt('/parsing':terminal('/parsing':word(!after)))))),dayTimePat(Now,DepartureTime)))))))).
annotation:shrink('/parsing':prod(queryPat(Now,forwardQuery(From,DepartureTime,To)),'/parsing':token,'/parsing':follow('/parsing':opt('/parsing':terminal('/parsing':word(!to))),'/parsing':follow(city(To),'/parsing':follow('/parsing':terminal('/parsing':word(!from)),'/parsing':follow(city(From),'/parsing':follow(annotation:parentheses('/parsing':alt('/parsing':alt('/parsing':terminal('/parsing':word(!on)),'/parsing':terminal('/parsing':word(!at))),'/parsing':follow('/parsing':terminal('/parsing':word(!departing)),'/parsing':opt('/parsing':terminal('/parsing':word(!after)))))),dayTimePat(Now,DepartureTime)))))))).
'/bootstrap':unitTest('/parsing':lexParseString(queryPat(dayTime(wednesday,time(19,43)),forwardQuery(!'new york',dayTime(wednesday,time(15,0)),!chicago)),!'new york to chicago at 3:00pm')).
'/bootstrap':unitTest('/parsing':lexParseString(queryPat(dayTime(wednesday,time(19,43)),forwardQuery(!seattle,dayTime(wednesday,time(7,30)),!denver)),!'denver from seattle departing after 7:30')).
